# T12 视频内容解析

## 目标

针对 `video` 站点，从选中详情项解析真实视频 URL。

## 已实现

- 新增 `VideoContentLoader` 抽象与运行时实现。
- 新增 `VideoContentPage`：
  - 异步加载视频 URL
  - 加载/错误/空态/展示态
- 连接详情页动作：
  - 视频站点：`Parse Video URL (T12)`
  - 漫画/图集站点仍走图片流程

## 边界

- 本步骤仅解析并展示视频 URL。
- 不包含视频播放接入（留到 `T13`）。
