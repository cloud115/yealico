# T11 图片阅读器

## 目标

提供基础漫画/图集阅读体验：

- 点击左右区域翻页
- 手势滑动翻页
- 页码显示

## 已实现

- 新增 `ImageReaderPage`：
  - 使用 `PageView` 支持滑动导航
  - 点击区域支持上一页/下一页
  - 底部页码指示（`current / total`）
  - 可缩放图片视图（`InteractiveViewer`）
- 图片内容页新增 `Open Reader (T11)` 入口。

## 边界

- 当前仅实现基础阅读行为。
- 本步骤不回写阅读历史。
- 本步骤不做高级预加载/缓存策略。
